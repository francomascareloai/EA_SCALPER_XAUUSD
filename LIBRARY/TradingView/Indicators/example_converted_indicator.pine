//@version=5
indicator("Example Converted Indicator", overlay=true)

// This is an example of a converted MQL4/5 indicator to Pine Script
// Original MQL4 code would have been converted to Pine Script format

// Input parameters
length = input.int(14, "Length")
source = input.source(close, "Source")

// Converted logic from MQL4
ma = ta.sma(source, length)
upperBand = ma + ta.stdev(source, length)
lowerBand = ma - ta.stdev(source, length)

// Plotting
plot(ma, "Moving Average", color=color.blue)
plot(upperBand, "Upper Band", color=color.red)
plot(lowerBand, "Lower Band", color=color.green)

// Fill between bands
fill(plot(upperBand), plot(lowerBand), color=color.new(color.purple, 90))