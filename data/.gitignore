# ==============================================================================
# DATA DIRECTORY .gitignore
# ==============================================================================
# See data/README.md for directory structure explanation

# Large raw data files (reproducible from FTMO CSVs)
raw/*.parquet
raw/*.csv

# All processed datasets (reproducible from raw/)
processed/by_session/*.parquet
processed/by_period/*.parquet
processed/experimental/*.parquet

# Historical backups (large, can be regenerated)
versions/*.parquet
versions/*.csv

# KEEP these files (small, critical):
!config.yaml
!README.md
!raw/*.metadata.json

# Keep directory structure
!**/.gitkeep
